<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#1A1A1A"
    android:orientation="vertical"
    android:padding="12dp">

    <!-- CABEÇALHO: Dia da semana e Cronômetro na mesma linha -->
    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content">

        <TextView
            android:id="@+id/widget_dia_semana"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_centerInParent="true"
            android:text="DOMINGO"
            android:textColor="#FFFFFF"
            android:textSize="10sp"
            android:textAllCaps="true" />

        <TextView
            android:id="@+id/widget_btn_cronometro"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_alignParentEnd="true"
            android:layout_centerVertical="true"
            android:text="⏱"
            android:textSize="20sp"
            android:textColor="#AAAAAA"
            android:padding="4dp" />
    </RelativeLayout>

    <!-- CONTEÚDO: Nome do grupo e Horário logo abaixo -->
    <TextView
        android:id="@+id/widget_nome_grupo"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:textAlignment="center"
        android:textColor="#FFFFFF"
        android:textSize="16sp"
        android:textStyle="bold"
        android:paddingBottom="2dp" />

    <TextView
        android:id="@+id/widget_horario_grupo"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:textAlignment="center"
        android:textColor="#FFFFFF"
        android:textSize="10sp"
        android:paddingBottom="8dp" />

    <TextView
        android:id="@+id/widget_lista_tarefas"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:textColor="#EEEEEE"
        android:textSize="14sp"
        android:lineSpacingExtra="4dp" />

</LinearLayout>