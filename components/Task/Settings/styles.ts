import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    settingsContainer: {
        height: 150,
        // margin: 12,
        padding: 15,
        backgroundColor: "#B7C5FE",
        borderRadius: 10,
        width: "40%",
        alignItems: "center",

        marginBottom: 10,
    },
});
